import { getArms } from "./database.js";

const arms = getArms();

document.addEventListener(
    "change", (event) => { 
        if (event.target.name === "arm"){
            
        }
    }
)
